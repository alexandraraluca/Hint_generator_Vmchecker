# --- Core data / utils ---
numpy>=1.26
pandas>=2.2
tqdm>=4.66
python-dateutil>=2.9
orjson>=3.10
pyyaml>=6.0

# --- File / archive handling ---
# zipfile is stdlib

# --- PDF parsing for statements.zip ---
pypdf>=4.2
pdfminer.six>=20240706

# --- Code parsing / AST ---
tree-sitter>=0.22
tree-sitter-languages>=1.10

# --- Plotting / EDA ---
matplotlib>=3.8
seaborn>=0.13
plotly>=5.20

# --- NLP / embeddings (used in retrieval & evaluation) ---
scikit-learn>=1.4
sentence-transformers>=3.0
rank-bm25>=0.2.2

# --- LLM client (Ollama for gpt-oss:20b) ---
ollama>=0.3.0
httpx>=0.27

# --- Dataset utilities ---
datasets>=2.20
jsonschema>=4.22

# --- Fine-tuning stack (Stage 4; install lazily on a GPU machine) ---
# torch and unsloth are best installed via their own instructions per platform.
# We declare transformers/peft here for tooling.
transformers>=4.44
# Streamlit's file watcher can import transformers vision stubs → torchvision
torchvision>=0.18
peft>=0.12
accelerate>=0.34
bitsandbytes>=0.43; platform_system != "Windows"
trl>=0.10

# --- App ---
streamlit>=1.36

# --- Dev ---
ruff>=0.5
ipykernel>=6.29
